<%= form_with(model: googlepay) do |form| %>
  <% if googlepay.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(googlepay.errors.count, "error") %> prohibited this googlepay from being saved:</h2>

      <ul>
        <% googlepay.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
